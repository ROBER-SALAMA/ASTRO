---
import MainLayout from "../layouts/MainLayout.astro";

// server only
const nombre: string = "Roberto Carlos";
---

<MainLayout title="Home page">

  <h1>{nombre}</h1>
  <h2>Home</h2>

  <p>Momento actual: {new Date().toLocaleTimeString()}</p>
  <p id="momemt">Momento actual real:</p>

  <footer>Año: 2023</footer>
  <script>
    // client only
    const currentYear: number = new Date().getFullYear();
    const footer = document.querySelector("footer") as HTMLElement;
    footer.innerText = `Año ${currentYear}`;

    //   const currenMoment: number = new Date().toLocaleDateString();
    const time = document.querySelector("#momemt") as HTMLParagraphElement;
    time.innerText = `Momento actual real ${new Date().toLocaleTimeString()}`;
  </script>
</MainLayout>
